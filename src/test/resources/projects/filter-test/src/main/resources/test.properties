prop.from.common=${prop.from.common}
prop.from.dev=${prop.from.dev}
prop.from.project=${prop.from.project}
prop.from.profile=${prop.from.profile}
prop.overridden=${prop.overridden}
db.driver=${db.driver}
db.url=${db.url}

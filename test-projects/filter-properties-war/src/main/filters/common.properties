# Common properties shared across all profiles
# These are loaded first and can be overridden by profile filters
app.name=FilterTestApp
app.version=2.5.0
db.driver=postgresql
db.pool.min=5
db.pool.max=20
cache.provider=infinispan
log.format=json
